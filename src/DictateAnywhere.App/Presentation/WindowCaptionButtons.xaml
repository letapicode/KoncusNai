<UserControl x:Class="DictateAnywhere.App.Presentation.WindowCaptionButtons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
             Focusable="False"
             shell:WindowChrome.IsHitTestVisibleInChrome="True">
  <StackPanel Orientation="Horizontal">
    <Button x:Name="MinimizeButton"
            Style="{DynamicResource AppChromeWindowButtonStyle}"
            ToolTip="Minimize"
            AutomationProperties.Name="Minimize"
            Click="OnMinimizeClicked">
      <TextBlock Style="{DynamicResource AppChromeGlyphTextBlockStyle}" Text="&#xE921;" />
    </Button>
    <Button x:Name="MaximizeRestoreButton"
            Style="{DynamicResource AppChromeWindowButtonStyle}"
            ToolTip="Maximize"
            AutomationProperties.Name="Maximize"
            Click="OnMaximizeRestoreClicked">
      <TextBlock x:Name="MaximizeRestoreGlyphTextBlock"
                 Style="{DynamicResource AppChromeGlyphTextBlockStyle}"
                 Text="&#xE922;" />
    </Button>
    <Button x:Name="CloseButton"
            Style="{DynamicResource AppChromeCloseButtonStyle}"
            ToolTip="Close"
            AutomationProperties.Name="Close"
            Click="OnCloseClicked">
      <TextBlock Style="{DynamicResource AppChromeGlyphTextBlockStyle}" Text="&#xE8BB;" />
    </Button>
  </StackPanel>
</UserControl>
