namespace DictateAnywhere.App.Presentation;

/// <summary>Display metadata only. The selectable message remains in the transcript document.</summary>
internal sealed record ChatUserBubble(bool IsPaper);
