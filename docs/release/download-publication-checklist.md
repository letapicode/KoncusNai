# Download publication checklist

- [ ] Publish `KoncusNai-Setup-Small-<version>-x64.exe` with SHA-256 and byte size.
- [ ] Publish `KoncusNai-<version>-x64.msi` with SHA-256 and byte size.
- [ ] Publish `KoncusNai-<version>-artifact-manifest.json`.
- [ ] Verify Authenticode signatures and timestamp chain on a clean Windows 11 x64 machine.
- [ ] State that provider models are prepared separately and may need first-use network access.
- [ ] Link the release notes, supported Windows version, known limitations, and privacy boundary.
